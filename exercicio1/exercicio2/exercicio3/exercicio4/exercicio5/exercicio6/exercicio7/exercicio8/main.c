#include <stdio.h>

/*
a) free libera a memória que foi alocada dinamicamente.

b) Não é recomendado usar o ponteiro após o free.

c) Vazamento de memória ocorre quando a memória
é alocada mas nunca liberada.

d) malloc retorna NULL quando não consegue alocar memória.

e) calloc aloca memória e inicializa todos os bytes com zero.

f) Diferença:

malloc -> não inicializa memória
calloc -> inicializa memória com zero
*/

int main(){
return 0;
}
